/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserMeDto } from '../../models/user-me-dto';

export interface Me$Plain$Params {
}

export function me$Plain(http: HttpClient, rootUrl: string, params?: Me$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<UserMeDto>> {
  const rb = new RequestBuilder(rootUrl, me$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserMeDto>;
    })
  );
}

me$Plain.PATH = '/api/Users/me';
