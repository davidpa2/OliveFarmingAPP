/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SeasonLitersDto } from '../../models/season-liters-dto';

export interface SeasonLiters$Plain$Params {
  seasonName?: string;
}

export function seasonLiters$Plain(http: HttpClient, rootUrl: string, params?: SeasonLiters$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<SeasonLitersDto>> {
  const rb = new RequestBuilder(rootUrl, seasonLiters$Plain.PATH, 'get');
  if (params) {
    rb.query('seasonName', params.seasonName, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SeasonLitersDto>;
    })
  );
}

seasonLiters$Plain.PATH = '/api/Rain/seasonLiters';
